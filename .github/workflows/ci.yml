# SPDX-FileCopyrightText: 2026 James Harton
#
# SPDX-License-Identifier: Apache-2.0

name: CI

on:
  push:
    branches: [main]
    tags: ["v*"]
  pull_request:
    branches: [main]

permissions:
  contents: write
  security-events: write
  pages: write
  id-token: write

jobs:
  CI:
    # TEMPORARY: tracking the branch of beam-bots/.github that adds `os-packages`
    # so it can be tested before it is merged. Back to a `main` SHA once it is.
    uses: beam-bots/.github/.github/workflows/elixir-ci.yml@ci/os-packages
    with:
      # `nerves_runtime` reaches `nerves_uevent`, which builds C against libmnl,
      # and `vintage_net_wifi` builds against libnl-genl. Both are optional
      # dependencies here, carried so dialyzer has their types.
      os-packages: libmnl-dev libnl-genl-3-dev
      enable-docs-deploy: true
      enable-release: true
    secrets:
      HEX_API_KEY: ${{ secrets.HEX_API_KEY }}
